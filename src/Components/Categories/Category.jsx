import React from "react"

const Category = (props) => {
   
   return <div>
           <div>props.categories</div>
           <div>props.title</div>
           <div>props.parentId</div>
           <div>props.createdAt</div>
   </div>
}

export default Category